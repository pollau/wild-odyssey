# Copy this file to .env and fill in the values
# .env is gitignored — never commit real values

# Umami Analytics (https://app.umami.is)
# Leave empty to disable tracking (local dev, PR previews, etc.)
PUBLIC_UMAMI_SITE_ID=
