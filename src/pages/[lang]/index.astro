---
import HomePage from "../../components/pages/HomePage.astro";

export function getStaticPaths() {
  return [{ params: { lang: "en" } }, { params: { lang: "es" } }];
}

const { lang } = Astro.params;
---

<HomePage lang={lang} audience="enterprise" />
